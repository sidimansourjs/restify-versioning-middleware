{
  "name": "restify-versioning-middleware",
  "version": "1.0.2",
  "description": "A module that offers a function that can be user as a restify.js middleware. The module will check incoming calls (as a PRE handler in Restify) and will throw an `InvalidVersionError` when no version has been received in neither URL not request headers.",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/*Test.js  --recursive",
    "postversion": "git push origin master && git push origin master --tags"
  },
  "keywords": [
    "restify",
    "versioning",
    "api",
    "rest",
    "middleware"
  ],
  "engines": {
    "node": "4.x"
  },
  "author": "SidiMansourJs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sidimansourjs/restify-versioning-middleware.git"
  },
  "dependencies": {
    "express-unless": "^0.3.0",
    "restify": "^4.1.1",
    "semver": "^5.1.0"
  },
  "devDependencies": {}
}
